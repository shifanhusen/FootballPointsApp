@model FootballPointsApp.ViewModels.LeaderboardViewModel

@{
    ViewData["Title"] = "Leaderboard";
}

<h1>Leaderboard - @Model.Month/@Model.Year</h1>

<div class="card shadow-sm border-0 mb-4">
    <div class="card-body bg-light rounded">
        <form method="get" class="row g-3 align-items-end">
            <div class="col-md-4">
                <label for="year" class="form-label fw-bold text-secondary">Year</label>
                <select class="form-select" name="year">
                    @for(int y = DateTime.Now.Year; y >= 2024; y--)
                    {
                        <option value="@y" selected="@(Model.Year == y)">@y</option>
                    }
                </select>
            </div>
            <div class="col-md-4">
                <label for="month" class="form-label fw-bold text-secondary">Month</label>
                <select class="form-select" name="month">
                    @for(int m = 1; m <= 12; m++)
                    {
                        <option value="@m" selected="@(Model.Month == m)">@System.Globalization.CultureInfo.CurrentCulture.DateTimeFormat.GetMonthName(m)</option>
                    }
                </select>
            </div>
            <div class="col-md-4">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="bi bi-filter"></i> Filter Leaderboard
                </button>
            </div>
        </form>
    </div>
</div>

<div class="card shadow border-0">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover table-striped mb-0 align-middle">
                <thead class="bg-primary text-white">
                    <tr>
                        <th class="py-3 ps-4">Rank</th>
                        <th class="py-3">Player</th>
                        <th class="py-3 text-center">Matches</th>
                        <th class="py-3 text-center" title="Attendance Points">Att. Pts</th>
                        <th class="py-3 text-center" title="Result Points (Win/Draw)">Res. Pts</th>
                        <th class="py-3 text-center" title="Goal Points">Goal Pts</th>
                        <th class="py-3 text-center" title="Bonus Points">Bonus</th>
                        <th class="py-3 text-center text-danger" title="Late Penalty">Late</th>
                        <th class="py-3 text-center text-danger" title="No Show Penalty">No Show</th>
                        <th class="py-3 text-center text-danger" title="No Response Penalty">No Resp</th>
                        <th class="py-3 pe-4 text-end fw-bold">Total</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Entries)
                    {
                        <tr class="@(item.Rank == 1 ? "table-warning" : "")">
                            <td class="ps-4 fw-bold">
                                @if(item.Rank == 1) { <i class="bi bi-crown-fill text-warning me-1"></i> }
                                @item.Rank
                            </td>
                            <td class="fw-medium">@item.PlayerName</td>
                            <td class="text-center">@item.MatchesPlayed</td>
                            <td class="text-center">@item.AttendancePoints.ToString("0.#")</td>
                            <td class="text-center">@item.ResultPoints.ToString("0.#")</td>
                            <td class="text-center">@item.GoalPoints.ToString("0.#")</td>
                            <td class="text-center text-success">@(item.BonusPoints > 0 ? "+" + item.BonusPoints.ToString("0.#") : "-")</td>
                            <td class="text-center text-danger">@(item.LatePenaltyPoints < 0 ? item.LatePenaltyPoints.ToString("0.#") : "-")</td>
                            <td class="text-center text-danger">@(item.NoShowPenaltyPoints < 0 ? item.NoShowPenaltyPoints.ToString("0.#") : "-")</td>
                            <td class="text-center text-danger">@(item.NoResponsePenaltyPoints < 0 ? item.NoResponsePenaltyPoints.ToString("0.#") : "-")</td>
                            <td class="pe-4 text-end fw-bold fs-5 text-primary">@item.TotalPoints.ToString("0.#")</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
